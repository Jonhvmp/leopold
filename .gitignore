# Leopold runtime state (per-project, created by /leopold-run)
.leopold/

# Local Claude Code settings
.claude/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Node (for the future SDK driver)
node_modules/
dist/
*.log

# Driver build artifact: the harness runtime vendored in at build time (copy-runtime.mjs)
packages/driver/assets/

# MkDocs build output
site/
.cache/

# Maintainer-only publishing + launch notes (kept local, not public)
PUBLISHING.md
launch/
