# awesome-claude-code PR draft

> Draft for the human to submit as a PR to a curated list (e.g. awesome-claude-code).
> Not submitted by Leopold — opening an external PR is a gated action. Match the list's
> exact format/category before submitting; this is the entry text only.

**List entry (one line):**

- [Leopold](https://github.com/Jonhvmp/leopold) — Autonomous orchestration harness: brief
  Claude Code like a teammate (MISSION/CHARTER/PLAN), it conducts a continuous run deciding
  from your charter, with a red-teamed git lock. MIT.

**If the list wants a short paragraph:**

Leopold turns a Claude Code session into an unattended run. You debate the mission into a
durable brief; a Stop hook keeps the agent going and a PreToolUse guard keeps git locked
(commit/push/`rm -rf`/publish blocked while autonomous, with a 59-case red-team suite in
CI). It conducts gstack's planning/QA skills when present and works without them. Includes
an optional external SDK driver. Alpha.

**Category suggestion:** Orchestration / Autonomous agents / Workflow.

**PR checklist (the human does this):**
- [ ] Fork the list repo, add the entry in the right category, alphabetical if required.
- [ ] Follow the list's CONTRIBUTING (some require a screenshot or a specific line format).
- [ ] Open the PR from your fork; keep the description short and factual.
